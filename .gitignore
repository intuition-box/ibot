# Dependencies
node_modules

# Secrets
.env

# Build output
dist

# Runtime state — the support database (ticket PII) lives here. Never commit.
data/db/

# Safety net: never commit a SQLite DB, wherever it lands.
*.db
*.db-shm
*.db-wal

# OS cruft
.DS_Store
**/.DS_Store

# Claude Code settings (personal permissions, machine-specific paths)
.claude/settings.local.json
.claude/settings.json
